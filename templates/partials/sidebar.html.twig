<div class="fixed top-0 left-0 w-64 h-full bg-gradient-to-b from-gray-800 to-gray-900 text-white mr-8 rounded-tr-2xl rounded-br-2xl">
    <!-- Logo -->
    <div class="p-4 border-b border-gray-700">
        <img src="/images/logo.svg" alt="Logo" class="h-8 mx-auto">
    </div>

    <!-- Navigation -->
    <nav class="p-4 space-y-6">
        <!-- Accueil -->
        <div>
            <a href="/" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_home' ? 'bg-gray-700 text-white' : 'text-gray-100 hover:bg-gray-700' }}">
                <i class="fas fa-home w-5"></i>
                <span class="ml-3">Tableau de bord</span>
            </a>
        </div>

        <!-- Outils -->
        <div class="space-y-2">
            <div class="px-4 text-xs font-semibold text-gray-400 uppercase">Outils</div>
            <a href="/tools" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_tools' ? 'bg-gray-700 text-white' : 'text-gray-100 hover:bg-gray-700' }}">
                <i class="fas fa-tools w-5"></i>
                <span class="ml-3">Catalogue des outils</span>
            </a>
        </div>

        <!-- Actualités -->
        <div class="space-y-2">
            <div class="px-4 text-xs font-semibold text-gray-400 uppercase">Actualités</div>
            <a href="/news" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_news' ? 'bg-gray-700 text-white' : 'text-gray-100 hover:bg-gray-700' }}">
                <i class="fas fa-newspaper w-5"></i>
                <span class="ml-3">Dernières actualités</span>
            </a>
            <a href="/rss" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_rss' ? 'bg-gray-700 text-white' : 'text-gray-100 hover:bg-gray-700' }}">
                <i class="fas fa-rss w-5"></i>
                <span class="ml-3">Flux RSS</span>
            </a>
        </div>

        <!-- Versions -->
        <div class="space-y-2">
            <div class="px-4 text-xs font-semibold text-gray-400 uppercase">Versions</div>
            <a href="/releases/latest" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_releases_latest' ? 'bg-gray-700 text-white' : 'text-gray-100 hover:bg-gray-700' }}">
                <i class="fas fa-code-branch w-5"></i>
                <span class="ml-3">Dernières versions</span>
            </a>
            <a href="/releases/subscribed" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'releases_subscribed' ? 'bg-gray-700 text-white' : 'text-gray-100 hover:bg-gray-700' }}">
                <i class="fas fa-star w-5"></i>
                <span class="ml-3">Versions suivies</span>
            </a>
        </div>
    </nav>
</div>