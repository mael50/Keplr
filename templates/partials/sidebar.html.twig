<div class="fixed top-0 left-0 w-64 h-full bg-gradient-to-b from-gray-100 to-white text-gray-800 mr-8 shadow-lg">
    <!-- Logo -->
    <div class="p-4 border-b border-gray-200">
        <img src="/logo_keplr.png" alt="Logo" class="mx-auto" style="width: 150px;">
    </div>

    <!-- Navigation -->
    <nav class="p-4 space-y-6">
        <!-- Accueil -->
        <div>
            <a href="/" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_home' ? 'bg-blue-100 text-blue-800' : 'text-gray-600 hover:bg-gray-100' }}">
                <i class="fas fa-home w-5"></i>
                <span class="ml-3">Tableau de bord</span>
            </a>
        </div>

        <!-- Outils -->
        <div class="space-y-2">
            <div class="px-4 text-xs font-semibold text-gray-500 uppercase">Outils</div>
            <a href="/tools" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_tools' ? 'bg-blue-100 text-blue-800' : 'text-gray-600 hover:bg-gray-100' }}">
                <i class="fas fa-tools w-5"></i>
                <span class="ml-3">Catalogue des outils</span>
            </a>
        </div>

        <!-- Actualités -->
        <div class="space-y-2">
            <div class="px-4 text-xs font-semibold text-gray-500 uppercase">Actualités</div>
            <a href="/news" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_article' ? 'bg-blue-100 text-blue-800' : 'text-gray-600 hover:bg-gray-100' }}">
                <i class="fas fa-newspaper w-5"></i>
                <span class="ml-3">Dernières actualités</span>
            </a>
            <a href="/rss" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_rss' ? 'bg-blue-100 text-blue-800' : 'text-gray-600 hover:bg-gray-100' }}">
                <i class="fas fa-rss w-5"></i>
                <span class="ml-3">Flux RSS</span>
            </a>
        </div>

        <!-- Versions -->
        <div class="space-y-2">
            <div class="px-4 text-xs font-semibold text-gray-500 uppercase">Versions</div>
            <a href="/releases/latest" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'app_releases_latest' ? 'bg-blue-100 text-blue-800' : 'text-gray-600 hover:bg-gray-100' }}">
                <i class="fas fa-code-branch w-5"></i>
                <span class="ml-3">Dernières versions</span>
            </a>
            <a href="/releases/subscribed" class="flex items-center px-4 py-2 rounded-lg transition-colors {{ app.request.attributes.get('_route') == 'releases_subscribed' ? 'bg-blue-100 text-blue-800' : 'text-gray-600 hover:bg-gray-100' }}">
                <i class="fas fa-star w-5"></i>
                <span class="ml-3">Versions suivies</span>
            </a>
        </div>
    </nav>
</div>